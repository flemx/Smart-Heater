syntax = "proto3";

package temperature;
service TemperatureService {

rpc getTemp(temp) returns (stream temp) {}
rpc setTemp(temp) returns (confirm) {}
 
}
message temp {
  int64 tempVal = 1;
}

message confirm {
    bool confirmed = 1;
  }
